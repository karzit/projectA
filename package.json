{
  "name": "@ccg/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Headless, deterministic rules engine for a custom Canvas card game, with a Canvas UI client.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:balance:live": "node scripts/run-balance-live.mjs",
    "test:balance:parallel": "node scripts/run-balance-parallel.mjs",
    "test:balance:live:parallel": "node scripts/run-balance-live-parallel.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  }
}
