
B까지 진행하겠습니다 (A → C → B). D(무승부)는 룰 결정이 필요하니 마지막에 따로 확인하겠습니다. 먼저 테스트를 읽어 스타일과 기존 커버리지를 확인하고, 깨지 않게 작업하겠습니다.


읽기 파일 3개
작업 계획이 명확해졌습니다. 진행하겠습니다.

A: 스탯 쓰기를 game.ts로 이동, effects.ts에서 state.units 직접 접근 제거
C: 죽은 keywords 살리기(인스턴스 경유), 낡은 주석/verb 주석 수정
B: firedForced/rituals 상태 추가 + forced.ts 평가 루프 + reducer 배선 + 테스트
기존 테스트(특히 modifyStat/swapStats 동작)는 그대로 통과하도록 유지합니다.

Rules 구조 개선 구현
Step A·C·B의 데이터 모델부터 — types.ts에 상태 필드와 ritual verb를 추가합니다.

수정됨
types.ts
+1
-0
이 변경은 헤드리스 룰 코어라 브라우저 검증 대상이 아닙니다 — npm test + npm run typecheck로 검증합니다. 계속 진행합니다.

수정됨
types.ts
+2
-0
Now game.ts — header comment, init new fields, and the new stat/ritual/forced mutations.

수정됨
game.ts
+6
-3
Now add the stat/ritual/forced-fired mutations to game.ts (before nextRandom):

수정됨
game.ts
+30
-0
Now queries.ts — keyword reads now go through the unit instance (살리기 + '*' 처리), plus the new read friends for the forced loop.

수정됨
queries.ts
+9
-2
Now add the hand/ritual/forced read friends to queries.ts:

수정됨
queries.ts
+20
-0
Now effects.ts — route stat writes through game.ts, remove state.units access, add the ritual verb. Step A's core fix:

수정됨
effects.ts
+5
-15
Now fix the misleading summonSelf/descend comment and add the ritual verb:

수정됨
effects.ts
+8
-4
Now the core of Step B — the new forced.ts evaluation loop:

생성됨
forced.ts
+102
-0
Now wire settleForced into the reducer (main phase only, before the loss check):

수정됨
reducer.ts
+6
-0
Now update the public API in index.ts:

수정됨
index.ts
+21
-1
핵심 코드를 마쳤습니다. 새 테스트를 쓰기 전에 먼저 타입체크와 기존 테스트로 회귀가 없는지 확인하겠습니다.

Ran TypeScript typecheck
Typecheck passes. Now run the existing rules tests:

Ran existing rules test suites
Regression in one combat test — and it's a correct consequence of wiring B: the test used traitor(배신자) merely as a high-power unit, but now that forced abilities auto-evaluate, after combat the lone traitor is the highest stat on its side, so its 배신자 ability fires and destroys itself. The combat test should use a forced-ability-free unit to test combat in isolation.

수정됨
rules-loop.test.ts
+4
-2
Now the new forced-ability test suite (Step B's verification). I'll build state directly to the main phase and drive settles via pass:


생성됨 파일 1개, 실행됨 명령 1개
All 6 pass. The 배신자 test's assertion is loose (an OR) because I didn't want to hard-code the RNG outcome. Let me check what the fixed seed actually does so I can make it a crisp assertion.

Probed deterministic 배신자 outcome across seeds
Path issue — let me put the probe inside the project so relative imports resolve:

Probed deterministic 배신자 outcome across seeds
Clean deterministic result with seed 1: the monkey is sacrificed and the traitor defects to B. Let me tighten the betray test to assert that concretely instead of the loose OR:

수정됨
rules-forced.test.ts
+6
-8
Now the full suite + typecheck together to confirm everything is green:

Ran full test suite and typechecked
전 테스트 54개(기존 48 + forced 6) 통과, 타입체크 클린. 이제 문서를 현재 구조에 맞게 갱신하겠습니다 — forced 루프가 "다음 단계"에서 "구현됨"으로 옮겨가야 합니다.

수정됨
README.md
+25
-5

서버가 일시적으로 요청을 제한했습니다
B까지 진행

거의 마무리 단계입니다 — A·C·B 코드와 테스트는 모두 통과했고, 남은 문서(레이어 표 + src/rules/CLAUDE.md + 루트 CLAUDE.md)만 정리하면 됩니다. 마저 갱신하겠습니다.


읽기 파일 1개, 편집됨 파일 1개
Now src/rules/CLAUDE.md — the layer map and the "known next steps":

수정됨
CLAUDE.md
+9
-4
Now the root CLAUDE.md open-items section, which still lists the forced loop as not built:


읽기 파일 1개, 편집됨 파일 1개
A·C·B 모두 완료했습니다. 마지막 전체 실행에서 54개 테스트 통과(기존 48 + forced 6), 타입체크 클린이었고, 이후 변경은 문서뿐이라 회귀 위험이 없습니다.

완료 내역
A — 쓰기 레이어 누수 제거
effects.ts에 있던 state.units[...] 직접 접근 3곳을 모두 제거. 스탯 변경/스왑이 이제 game.ts의 mutation을 거칩니다.

game.ts: modifyStat, swapStats 추가
effects.ts:89-96: 쿼리로 읽고 game 호출로 씀 → state.units 참조 0
C — 죽은 필드 살리기 + 정리
UnitInstance.keywords 활성화: 키워드 검사를 def가 아닌 인스턴스 경유로 전환(queries.ts의 unitHasKeyword, '*' all-마커 처리). 힘/지혜처럼 키워드도 변이 가능한 일관된 모델이 됨.
summonSelf/descend의 오해 소지 주석 수정, game.ts 낡은 헤더 주석 갱신.
B — forced-ability 평가 루프 (핵심 구조 신규)
상태 모델 확장(types.ts): GameState.firedForced, GameState.rituals, ritual effect verb.
forced.ts 신규: 메인 페이즈 액션마다 fixpoint로 settle, loss 판정 전에 실행 → 복수자가 빈 필드에서 부활해 패배를 막음. hand(복수자/마왕)와 field(배신자) 모두 스캔, once는 firedForced 기록, no-op 루프는 per-settle seen + SETTLE_LIMIT로 차단. 결정적 순서(A→B, hand→field).
game.ts: performRitual, markForcedFired.
reducer.ts:42: isMainPhase일 때만 settleForced 배선.
테스트 6종 추가(tests/rules-forced.test.ts): 복수자 부활/비부활, 배신자 처치+배신(seed 고정 결정적), once 1회, 마왕 의식 5회 강림/미달.
문서(README.md, 양쪽 CLAUDE.md)도 레이어 표·verb 목록·next steps를 현재 구조에 맞게 갱신했습니다.

작업 중 드러난 설계 결정 (확인 필요)
forced 평가 시점 — 오프닝 중엔 자동 소환이 배치를 방해하므로 메인 페이즈 한정으로 기본값을 잡았습니다. (기존 combat 테스트 1개가 이 영향을 받아, traitor를 forced 없는 monkey-king으로 바꿔 전투를 격리했습니다 — 의도는 보존.)
부활 의식을 실제로 무엇이 올리나 — 카운터·verb·performRitual은 준비됐지만, 실전에서 어떤 카드/이벤트가 의식을 수행하는지는 미정으로 남겨 README에 명시했습니다. (룰 결정 사항)
남은 항목 D (무승부) — 미진행
양쪽 필드가 동시에 비면 checkLoss가 항상 A를 패자로 처리합니다. 무승부 모델링은 룰 결정이 필요해 B 범위에서 제외했습니다. 동시 소멸을 무승부(draw)로 둘지, 아니면 현재처럼 한쪽을 패자로 둘지 정해주시면 이어서 처리하겠습니다.

1. 오프닝 배치시 모든 효과는 오프닝 종료 후 발동(턴 플레이어 순서대로) 선턴 플레이어 이점 추가가 필요함

1. 오프닝 배치시 모든 효과는 오프닝 종료 후 발동(턴 플레이어 순서대로) 선턴 플레이어 이점 추가가 필요함

오프닝 배치 시 효과 지연(deferred effects) 구조가 필요합니다. 설계:

placeOpening → 유닛 소환/카드 제거만 즉시 처리, 효과를 state에 큐잉
maybeStartMain → A 큐 → B 큐 순으로 일괄 발동
이후 forced settle → loss check 순서는 기존과 동일
GameState에 openingEffects 필드가 필요하니 모델부터 시작합니다.


편집됨 파일 2개
이제 reducer에서 placeOpening을 효과 지연 버전으로 교체하고, maybeStartMain에서 A→B 순으로 일괄 발동합니다.

수정됨
reducer.ts
+42
-10
index.ts에 DeferredEffect 내보내기 추가:

수정됨
index.ts
+1
-1
타입체크 먼저 실행합니다.

실행됨
타입체크
클린. 기존 테스트를 실행합니다.

실행됨
전체 테스트 실행
54개 전부 통과. 이제 새 동작을 검증하는 테스트를 추가합니다 — 효과 지연 확인, A→B 순서 확인, 배경 조건(유닛은 즉시 필드에 있어야 함) 확인.


읽기 파일 1개, 편집됨 파일 1개, 실행됨 명령 1개
A가 우공이산 배치, B가 finishOpening만 했을 때 A의 openingDone이 아직 false라 메인 미진입. 테스트 흐름 수정:


편집됨 파일 1개, 실행됨 명령 1개
57개 전부 통과. README의 오프닝 설명도 갱신합니다.

수정됨
README.md
+6
-3
완료입니다. 변경 요약:

완료 내역
오프닝 효과 지연 + 선턴 이점

파일	변경
types.ts	DeferredEffect 타입 추가, GameState.openingEffects 필드 추가
game.ts	createGame에서 openingEffects: { A: [], B: [] } 초기화
reducer.ts	placeOpeningCard 신규 — 유닛은 즉시 소환, 효과는 큐에 쌓음; maybeStartMain — 오프닝 종료 시 A→B 순 일괄 발동
tests/rules-loop.test.ts	효과 지연 확인, 유닛 즉시 필드 등록(배경 조건), A→B 순서 검증 3개 케이스 추가
README.md	오프닝 규칙 설명 갱신
핵심 동작:

오프닝 배치 시 유닛은 즉시 필드에 올라옴 → 이후 카드의 배경 조건 충족 가능
효과(develops + effects)는 큐에 보관되어 오프닝 종료까지 발동 안 됨
양측 오프닝이 끝나면: A의 큐 → B의 큐 순서로 일괄 발동 → forced settle → loss check
테마:영웅담
1.모험의 시작[전개:지역:시작의 마을, 적 전장에 1/1 슬라임을 2마리 소환합니다.]
용사[5/5, 환경이 변화할 때마다 1/1을 얻습니다.]
지역:시작의 마을
슬라임[1/0, 작고 연약합니다.]

2.기본 체력물약[내 유닛 하나가 이번 턴 동안 2/0을 얻습니다.]

3.퀘스트 - 슬라임 토벌[전개:장소:슬라임 동굴, 적 전장에 슬라임 3마리와 킹슬라임 1마리를 소환합니다.]
장소:슬라임 동굴
슬라임[1/0, 작고 연약합니다.]
킹슬라임[7/3, 지략 4, 다른 슬라임이 사망할 때 마다 +1/+1을 얻습니다. 최후:적 용사에게 지략2를 부여합니다.]
지략X:해당 유닛의 지략보다 낮은 지혜 배경을 취소할 수 있습니다.(한 턴에 한 번만!)
......

테마:서유기
1.미후왕[7/7, 적을 공격할 수 없습니다. 배경:장소:산, 내 턴마다 패악질을 일삼습니다, 진행:손오공]
진행[모든 상태를 유지한 상태로 다른 카드가 됩니다.]
패악질[3가지 효과중 무작위 하나를 발동합니다.]{
    이번 턴 동안 내 패의 무작위 카드 1장을 낼 수 없습니다,
    다른 무작위 유닛을 공격합니다(아군일 경우 파괴되지는 않고 이 유닛의 힘과 지혜만큼 힘과 지혜를 잃습니다.)
    다른 모든 아군 유닛에게 -2/0을 부여합니다.
}
손오공[12/12, 적을 공격할 수 없습니다. 내 턴마다 패악질을 모두 일삼습니다, 진행:필마온]
필마온[13/9, 적을 공격할 수 없습니다. 내 턴 시작시 패악질을 부리고 진행합니다, 진행:제천대성]
제천대성[15/15, 적을 공격할 수 없습니다. 내 턴 시작시 패악질을 모두 부리고 오행산에 갇혀버립니다, 진행:손행자]
손행자[15/15, 삼장법사가 전장에서 이탈할 경우 같이 이탈합니다, 진행:투전승불]
투전승불[20/20, 내 유닛들의 지혜의 총합만큼 지략을 얻습니다.]
2.수보리조사[4/8, 미후왕을 진행시키고 게임을 떠납니다.]
......


/compact

Compacted
/

