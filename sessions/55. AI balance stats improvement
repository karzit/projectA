
카드의 평균 등장 턴과 유닛의 평균 생존 시간 기록(내고 바로 상대가 잡으면 0턴 생존)


읽기 파일 2개, 검색함 코드
Now implementing: test-file tracking of appearance turn + unit survival, then propagating through the parallel merge script and dashboard.

수정됨
ai-balance.test.ts
+50
-12
Now update runOneGame, GameOutcome, and the call site:


읽기 파일 1개, 편집됨 파일 1개
Now update the call site and shard write/merge:

수정됨
ai-balance.test.ts
+2
-1
Now writeShardStats already spreads liveCardStats (as cardStats), which already includes the new fields automatically. Let's confirm and check scripts/run-balance-parallel.mjs merge logic needs updating for the new fields:

수정됨
run-balance-parallel.mjs
+4
-1
Now update the dashboard's card table (add columns for avg appearance turn and avg survival) and the buildAiSummary JSON export:

수정됨
ai-balance-dashboard.html
+9
-2
Now let's typecheck and run a quick smoke test to make sure the tracking logic doesn't crash.

실행됨
명령
Typechecks clean. Now a quick smoke run of the balance test to verify it doesn't crash and the new numbers come out sane.

실행됨
명령
I'll wait for this to finish, then check the produced stats/ai-balance-stats.json for sane turnSum/survivalSum values before reporting back.

백그라운드 셸 완료
SIM_GAMES=1 npx vitest run tests/ai-balance.test.ts 2&gt;&amp;1 | tail -60

실행됨 명령 2개
Working correctly — spell/non-unit cards correctly show no survival data, unit cards show sensible numbers (e.g. stone-monkey avg survival 13.7 turns, subori-josa/monkey-king at exactly 1 turn — evolving away almost immediately once played, which tracks with the evolve-chain timing). Typecheck is clean and the sim runs without errors.

실행됨
명령
완료했습니다. 대시보드/JSON에 카드별로 두 지표가 추가됐습니다:

평균 등장 턴 (avgAppearTurn) — 그 카드가 게임당 처음 등장한 턴의 평균
평균 생존 턴 (avgSurvivalTurns) — 필드에 있던 유닛이 죽거나(즉시 죽으면 0턴) 진화로 형태가 바뀔 때까지 살아있던 턴 수 평균 (스펠류 카드는 유닛이 아니라서 -로 표시)
스모크 테스트로 확인해보니 값도 말이 됩니다 — 예: subori-josa/monkey-king은 평균 생존 1턴(놓자마자 바로 다음 형태로 진화해버림), cultist는 26턴(오래 버팀), g-teacher는 5턴인데 표본(survivalCount)이 560개나 되는 걸 보면 자주 나고 자주 죽는 소모품 성격.

이제 node scripts/run-balance-parallel.mjs로 실행하신 통계 파일을 대시보드로 열어보시면 카드 테이블에 두 컬럼이 바로 보일 겁니다. 다음 통계 주시면 그걸로 실제 병목(어떤 카드가 놓자마자 죽는지 등) 다시 봐드리겠습니다.


읽기 파일 1개, 검색함 코드
새 지표들 덕분에 병목이 훨씬 선명하게 보입니다.

journey 체인 (원숭이 진화):
monkey-king(1.33턴 등장)→son-wukong(2.33)→pilmaon(3.34)→je-cheon-dae-sung(5.34)→son-haengja(8.53) — 여기까진 순조롭게 다 도달합니다(각 카드 games=73~79/79, 거의 전 게임에서 도달). son-haengja도 평균 8.33턴이나 살아남고요. 그런데 마지막 단계(tu-jeon-seung-bul)는 거의 안 나옵니다(완주 2/79).

원인을 코드에서 찾아보니: son-haengja→tu-jeon-seung-bul은 카드 효과가 아니라 tang-monk(삼장법사)가 매 턴 cell-1씩 이동하다 cell 0에 도달하는 순간 아군 전체를 진화시키는 구조입니다(Board.ts:journeyStep, "다음 셀이 비어있어야만 이동, 막히면 그 자리에서 멈춤"). tang-monk는 cell 4에서 시작해 4칸을 걸어가야 하고, 평균 9.33턴이나 살아있으니 시간은 충분한데 — 전열 0~4번 칸이 다른 아군 유닛들로 계속 채워지면서 길이 막혀 이동 자체가 멈춰버리는 게 실제 병목으로 보입니다.

이건 카드 밸런스보다는 "AI가 유닛을 배치/이동할 때 tang-monk의 진행 경로(cell 4→0)를 비워두도록 신경 써야 하는" 문제라서, DeckStrategy/MctsAI의 배치 로직에 손을 대야 합니다 — 확인해볼까요, 아니면 다른 우선순위로 넘어갈까요?

삼장법사 그 자동이동 없애버리고 알아서 공격/이동 가능하게 만들어

삼장법사 그 자동이동 없애버리고 공격/이동 가능한 일반 유닛으로 만들어


검색함 코드, 읽기 파일 1개, 편집됨 파일 2개
Existing tests reference the old journeyStep API. Let me look at that test file to update it properly.

읽기
rules-journey.test.ts
Now let's rewrite these tests to match the new "manual move + turnEnd arrival check" behavior.


편집됨 파일 1개, 검색함 코드
Good, it's public. Now let's typecheck and run the journey/rules tests plus a check for other TangMonk-related decks (opening cell 4 placement default in decks.ts or MctsAI opening logic doesn't force-place tang-monk).


실행됨 명령 2개
Clean. Now run the rules-journey test suite and full rules tests to confirm nothing else broke.


실행됨 명령 10개, 검색함 코드, 읽기 파일 2개
Now fixing all these unrelated fixtures to place tang-monk off cell 0 (avoiding the new "completes journey if sitting at cell 0 at turn end" behavior):


편집됨 파일 1개, 검색함 코드, 실행됨 명령 3개, 읽기 파일 1개
Now fix the D-2 test's playViaAction for tang-monk to use an explicit non-zero cell:


읽기 파일 1개, 편집됨 파일 1개
Now re-run the full journey test suite plus a broader rules test pass to confirm everything's green.


실행됨 명령 1개, 읽기 파일 2개, 편집됨 파일 1개
Now re-run the full test suite and typecheck:

실행됨
명령
All 36 pass and typecheck is clean. Now let's run the full test suite once more to be sure nothing else in the repo depended on the old journeyStep behavior.

실행됨
명령
All 172 rules tests pass, typecheck is clean. Summary of the change:

src/rules:

Board.ts — journeyStep (forced cell-by-cell movement) replaced with checkJourneyArrival, which only checks "is this unit at cell 0" and fires the same evolve-all-allies effect. No movement logic left in the engine.
TangMonk.ts — onPlay no longer force-moves it to cell 4 (still releases a trapped ally). subscribe now checks arrival on turnEnd instead of forcing a move on turnStart. Tang-monk is otherwise a plain unit (no cannotAttack/cannotMove) — player attacks/moves it manually, and if it's sitting on cell 0 when its controller's turn ends, the journey completes.
Tests: updated rules-journey.test.ts for the new manual-movement model, plus fixed a few unrelated fixtures that incidentally placed tang-monk at cell 0 (which now triggers instant completion at turn end — moved them to cell 1/3 as appropriate for lane reachability). 172/172 rules tests pass, typecheck clean.

This should let the AI (and a human player) walk tang-monk into position using normal move actions, or even attack with it along the way, instead of being locked into a scripted march that stalled out whenever the front row got crowded. Want me to run the balance sim again to see if journey's completion rate moves now, or hand it back to you to test live?

내가 굴려봄

알겠습니다. 결과 통계 파일 주시면 다시 확인하겠습니다.



